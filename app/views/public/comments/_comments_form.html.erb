<%= form_with model: [post, comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '1', class: "form-control", placeholder: "メッセージをここに(120文字)" %>
  <%= f.submit "メッセージを届ける", class:"btn btn-outline-info mt-2" %>
<% end %>