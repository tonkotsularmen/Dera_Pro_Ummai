<main class="page-post-new">
  <div class="container-xl">
    <!--最初は表示、lgのサイズになったら消える-->
    <div class="row d-lg-none my-2 mx-4">
      <div class="col p-0">
        <div class="row ">
          <div class="border-bottom"><h2>やりたくないフォーム</h2></div>
        </div>
        <div class="row">
          <%= form_with model: @zasetsu do |f| %>
            <div class="mt-3">
              <div class="pb-4">
                <h4 class="mt-3 border-bottom">画像<em>(jpegまたはpng形式)</em></h4>
                <%= f.file_field :image, accept: "image/*", required: true %>
              </div>
              <div class="pb-4">
                <h4 class="mt-3 border-bottom">タイトル</h4>
                <%= f.text_field :title, class:"form-control", required: true %>
              </div>
              <div class="pb-4">
                <h4 class="mt-3 border-bottom">説明</h4>
                <%= f.text_area :caption, class:"form-control", required: true %>
              </div>
              <%= f.submit '投稿' %>
            </div>
          <% end %>
        </div>
        <div class="row">
          <div class="mt-2">下記画像をダウンロードし、ファイルを添付して投稿してください。</div>
          <div class="mt-2"><%= button_to 'ファイルのダウンロード', download_yaruki_path, method: :get,
                            class: "btn btn btn-outline-secondary" %></div>
          <div class="mt-3">ダウンロードが不安な方は以下の画像をスクショして投稿してください。</div>
          <div class="mt-4"><%= image_tag 'IMG_0924.jpg', size: '400x300'%></div>
        </div>
      </div>
    </div>

    <div class="row">
      <!--最初は消えてて、lgのサイズになったらcol-4のサイズで表示-->
      <div class="d-none d-lg-block col mt-3">
        <div class="row d-flex justify-content-center">
          <div class="border-bottom"><h2>食欲やばいフォーム</h2></div>
        </div>
        <div class="row">
          <div class="col-4 p-0">
            <%= form_with model: @zasetsu do |f| %>
              <div class="mt-3">
                <div class="pb-4">
                  <h4 class="mt-3 border-bottom">画像<em>(jpegまたはpng形式)</em></h4>
                  <%= f.file_field :image, accept: "image/*", required: true %>
                </div>
                <div class="pb-4">
                  <h4 class="mt-3 border-bottom">タイトル</h4>
                  <%= f.text_field :title, class:"form-control", required: true %>
                </div>
                <div class="pb-4">
                  <h4 class="mt-3 border-bottom">説明</h4>
                  <%= f.text_area :caption, class:"form-control", required: true %>
                </div>
                <%= f.submit '投稿' %>
              </div>
            <% end %>
          </div>
          <div class="offset-2 col-6">
            <div class="mt-5 pt-2">下記画像をダウンロードし、ファイルを添付して投稿してください。</div>
            <div class="mt-2"><%= button_to 'ファイルのダウンロード', download_yaruki_path, method: :get,
                              class: "btn btn btn-outline-secondary" %></div>
            <div class="mt-3">ダウンロードが不安な方は以下の画像をスクショして投稿してください。</div>
            <div class="mt-4"><%= image_tag 'IMG_0924.jpg', size: '400x300'%></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>